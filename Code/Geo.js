/*const centerdot = document.getElementById('centerdot')
const centercoords = centerdot.getBoundingClientRect()
console.log(centercoords.x,centercoords.y)

document.addEventListener('click', function (event) {
    const coordinatesElement = document.getElementById('coordinates');
    const x = event.clientX;
    const y = event.clientY;
    const distance = Math.sqrt((centercoords.x-x)**2+(centercoords.y-y)**2).toFixed(2)
    coordinatesElement.textContent = `Clicked at coordinates: (${x}, ${y}). Distance from the actual location is ` + distance;
    });
*/

